Place the data file in this directory
(There is no need to specify the number of features or datapoints)

In the terminal, change the working directory to this directory and use the command:
"ant compile jar run"
to run the program

Specify the name of the data file in at the first input pane and the other paramters
of the program in the rest